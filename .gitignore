# Results and scratch
results/
work/
input/
logs/*
!logs/.gitkeep
.snakemake/

# Fetched / built resources — see scripts/fetch_reference.sh, scripts/build_indices.sh
resources/reference/
resources/gdna_index/
resources/locus_index/
*.bt2
*.fa.fai
*.fa.gz.fai
*.fa.gz.gzi
*.dict
*.cram
*.crai
*.bam
*.bai

# Sample sheets are cohort-specific
config/samples.tsv
config/manifest.tsv
# The full 2,504-sample collection is regenerated by make_manifest.py; the two
# 100-sample draws below it are pinned because a report cites them.
config/manifest.2504.tsv
# A staged manifest points at slices on this machine's scratch, so it is a site
# artefact rather than a cohort definition. Regenerate with scripts/stage_slices.py.
config/*.staged.tsv

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.venv/
venv/

# Editors / OS
.DS_Store
*.swp
.claude/
